import { colors } from "./tokens/colors";
import { breakpoints } from "./tokens/breakpoints";
import { sizes } from "./tokens/sizes";
import { transitions } from "./tokens/transitions";
import { motions } from "./tokens/motions";

export const theme = {
  colors,
  breakpoints,
  sizes,
  transitions,
  motions
} as const